# URL do Vite do local-app em desenvolvimento (terminal: cd desktop/local-app && yarn dev).
# Não confundir com o URL do deployment WorkAdventure (ex.: escritorio.vmix.chat) — esse regista-se como "servidor" na própria app.
LOCAL_APP_URL=http://localhost:3000
